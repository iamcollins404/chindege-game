import { motion } from 'framer-motion';

export const Controls = ({ isFlying, gameOver, multiplier, onStart, onCashOut, canStart, betAmount }) => {
  return null; // We've moved the controls into GameArea
};